server:
  port: 8082
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/ThiagoLuzFerreira/configs
          username: ${GITHUB_USER}
          password: ${GITHUB_PASS}
